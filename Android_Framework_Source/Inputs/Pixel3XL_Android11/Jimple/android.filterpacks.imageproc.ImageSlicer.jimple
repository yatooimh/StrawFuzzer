public class android.filterpacks.imageproc.ImageSlicer extends android.filterfw.core.Filter
{
    private int mInputHeight;
    private int mInputWidth;
    private android.filterfw.core.Frame mOriginalFrame;
    private int mOutputHeight;
    private int mOutputWidth;
    private int mPadSize;
    private android.filterfw.core.Program mProgram;
    private int mSliceHeight;
    private int mSliceIndex;
    private int mSliceWidth;
    private int mXSlices;
    private int mYSlices;

    public void <init>(java.lang.String)
    {
        android.filterpacks.imageproc.ImageSlicer r0;
        java.lang.String r1;

        r0 := @this: android.filterpacks.imageproc.ImageSlicer;

        r1 := @parameter0: java.lang.String;

        specialinvoke r0.<android.filterfw.core.Filter: void <init>(java.lang.String)>(r1);

        r0.<android.filterpacks.imageproc.ImageSlicer: int mSliceIndex> = 0;

        return;
    }

    private void calcOutputFormatForInput(android.filterfw.core.Frame)
    {
        android.filterpacks.imageproc.ImageSlicer r0;
        android.filterfw.core.Frame r1;
        android.filterfw.core.FrameFormat $r2, $r3;
        int $i0, $i1, $i2, $i3, $i4, $i5, $i6, $i7, $i8, i9, i10, i11, i12, i13, i14, i15;

        r0 := @this: android.filterpacks.imageproc.ImageSlicer;

        r1 := @parameter0: android.filterfw.core.Frame;

        $r2 = virtualinvoke r1.<android.filterfw.core.Frame: android.filterfw.core.FrameFormat getFormat()>();

        $i0 = virtualinvoke $r2.<android.filterfw.core.FrameFormat: int getWidth()>();

        r0.<android.filterpacks.imageproc.ImageSlicer: int mInputWidth> = $i0;

        $r3 = virtualinvoke r1.<android.filterfw.core.Frame: android.filterfw.core.FrameFormat getFormat()>();

        i9 = virtualinvoke $r3.<android.filterfw.core.FrameFormat: int getHeight()>();

        r0.<android.filterpacks.imageproc.ImageSlicer: int mInputHeight> = i9;

        i10 = r0.<android.filterpacks.imageproc.ImageSlicer: int mInputWidth>;

        i11 = r0.<android.filterpacks.imageproc.ImageSlicer: int mXSlices>;

        $i1 = i10 + i11;

        $i2 = $i1 - 1;

        i12 = $i2 / i11;

        r0.<android.filterpacks.imageproc.ImageSlicer: int mSliceWidth> = i12;

        i13 = r0.<android.filterpacks.imageproc.ImageSlicer: int mYSlices>;

        $i3 = i9 + i13;

        $i4 = $i3 - 1;

        i14 = $i4 / i13;

        r0.<android.filterpacks.imageproc.ImageSlicer: int mSliceHeight> = i14;

        i15 = r0.<android.filterpacks.imageproc.ImageSlicer: int mPadSize>;

        $i5 = i15 * 2;

        $i6 = i12 + $i5;

        r0.<android.filterpacks.imageproc.ImageSlicer: int mOutputWidth> = $i6;

        $i7 = i15 * 2;

        $i8 = i14 + $i7;

        r0.<android.filterpacks.imageproc.ImageSlicer: int mOutputHeight> = $i8;

        return;
    }

    public android.filterfw.core.FrameFormat getOutputFormat(java.lang.String, android.filterfw.core.FrameFormat)
    {
        android.filterpacks.imageproc.ImageSlicer r0;
        java.lang.String r1;
        android.filterfw.core.FrameFormat r2;

        r0 := @this: android.filterpacks.imageproc.ImageSlicer;

        r1 := @parameter0: java.lang.String;

        r2 := @parameter1: android.filterfw.core.FrameFormat;

        return r2;
    }

    public void process(android.filterfw.core.FilterContext)
    {
        android.filterpacks.imageproc.ImageSlicer r0;
        android.filterfw.core.FilterContext r1;
        int i0, i1, i2, $i3, $i4, $i5, $i6, $i7, $i8, $i9, $i10, $i11, $i12, $i13, $i14, $i15, $i16, $i17, i18, i19, i20, i21, i22;
        android.filterfw.core.Frame $r2, $r8, $r10, $r13, r16, r18;
        android.filterfw.core.FrameFormat $r3;
        android.filterfw.core.FrameManager $r4;
        android.filterfw.core.Program $r5, $r6, $r9;
        float $f0, $f1, $f2, $f3, $f4, $f5, $f6, $f7, f8, f9, f10, f11;
        android.filterfw.core.ShaderProgram $r7, $r15;
        android.filterfw.core.MutableFrameFormat r17;

        r0 := @this: android.filterpacks.imageproc.ImageSlicer;

        r1 := @parameter0: android.filterfw.core.FilterContext;

        $i3 = r0.<android.filterpacks.imageproc.ImageSlicer: int mSliceIndex>;

        if $i3 != 0 goto label1;

        r16 = virtualinvoke r0.<android.filterpacks.imageproc.ImageSlicer: android.filterfw.core.Frame pullInput(java.lang.String)>("image");

        r0.<android.filterpacks.imageproc.ImageSlicer: android.filterfw.core.Frame mOriginalFrame> = r16;

        specialinvoke r0.<android.filterpacks.imageproc.ImageSlicer: void calcOutputFormatForInput(android.filterfw.core.Frame)>(r16);

     label1:
        $r2 = r0.<android.filterpacks.imageproc.ImageSlicer: android.filterfw.core.Frame mOriginalFrame>;

        $r3 = virtualinvoke $r2.<android.filterfw.core.Frame: android.filterfw.core.FrameFormat getFormat()>();

        r17 = virtualinvoke $r3.<android.filterfw.core.FrameFormat: android.filterfw.core.MutableFrameFormat mutableCopy()>();

        $i5 = r0.<android.filterpacks.imageproc.ImageSlicer: int mOutputWidth>;

        $i4 = r0.<android.filterpacks.imageproc.ImageSlicer: int mOutputHeight>;

        virtualinvoke r17.<android.filterfw.core.MutableFrameFormat: void setDimensions(int,int)>($i5, $i4);

        $r4 = virtualinvoke r1.<android.filterfw.core.FilterContext: android.filterfw.core.FrameManager getFrameManager()>();

        r18 = virtualinvoke $r4.<android.filterfw.core.FrameManager: android.filterfw.core.Frame newFrame(android.filterfw.core.FrameFormat)>(r17);

        $r5 = r0.<android.filterpacks.imageproc.ImageSlicer: android.filterfw.core.Program mProgram>;

        if $r5 != null goto label2;

        $r15 = staticinvoke <android.filterfw.core.ShaderProgram: android.filterfw.core.ShaderProgram createIdentity(android.filterfw.core.FilterContext)>(r1);

        r0.<android.filterpacks.imageproc.ImageSlicer: android.filterfw.core.Program mProgram> = $r15;

     label2:
        i18 = r0.<android.filterpacks.imageproc.ImageSlicer: int mSliceIndex>;

        i0 = r0.<android.filterpacks.imageproc.ImageSlicer: int mXSlices>;

        i1 = i18 / i0;

        i2 = r0.<android.filterpacks.imageproc.ImageSlicer: int mSliceWidth>;

        i19 = r0.<android.filterpacks.imageproc.ImageSlicer: int mPadSize>;

        $i6 = i18 % i0;

        $i7 = i2 * $i6;

        $i8 = $i7 - i19;

        f8 = (float) $i8;

        i20 = r0.<android.filterpacks.imageproc.ImageSlicer: int mInputWidth>;

        $f0 = (float) i20;

        f9 = f8 / $f0;

        $i9 = r0.<android.filterpacks.imageproc.ImageSlicer: int mSliceHeight>;

        $i10 = $i9 * i1;

        $i11 = $i10 - i19;

        f10 = (float) $i11;

        i21 = r0.<android.filterpacks.imageproc.ImageSlicer: int mInputHeight>;

        $f1 = (float) i21;

        f11 = f10 / $f1;

        $r6 = r0.<android.filterpacks.imageproc.ImageSlicer: android.filterfw.core.Program mProgram>;

        $r7 = (android.filterfw.core.ShaderProgram) $r6;

        $i12 = r0.<android.filterpacks.imageproc.ImageSlicer: int mOutputWidth>;

        $f3 = (float) $i12;

        $f2 = (float) i20;

        $f7 = $f3 / $f2;

        $i13 = r0.<android.filterpacks.imageproc.ImageSlicer: int mOutputHeight>;

        $f5 = (float) $i13;

        $f4 = (float) i21;

        $f6 = $f5 / $f4;

        virtualinvoke $r7.<android.filterfw.core.ShaderProgram: void setSourceRect(float,float,float,float)>(f9, f11, $f7, $f6);

        $r9 = r0.<android.filterpacks.imageproc.ImageSlicer: android.filterfw.core.Program mProgram>;

        $r8 = r0.<android.filterpacks.imageproc.ImageSlicer: android.filterfw.core.Frame mOriginalFrame>;

        virtualinvoke $r9.<android.filterfw.core.Program: void process(android.filterfw.core.Frame,android.filterfw.core.Frame)>($r8, r18);

        $i14 = r0.<android.filterpacks.imageproc.ImageSlicer: int mSliceIndex>;

        i22 = $i14 + 1;

        r0.<android.filterpacks.imageproc.ImageSlicer: int mSliceIndex> = i22;

        $i16 = r0.<android.filterpacks.imageproc.ImageSlicer: int mXSlices>;

        $i15 = r0.<android.filterpacks.imageproc.ImageSlicer: int mYSlices>;

        $i17 = $i16 * $i15;

        if i22 != $i17 goto label3;

        r0.<android.filterpacks.imageproc.ImageSlicer: int mSliceIndex> = 0;

        $r13 = r0.<android.filterpacks.imageproc.ImageSlicer: android.filterfw.core.Frame mOriginalFrame>;

        virtualinvoke $r13.<android.filterfw.core.Frame: android.filterfw.core.Frame release()>();

        virtualinvoke r0.<android.filterpacks.imageproc.ImageSlicer: void setWaitsOnInputPort(java.lang.String,boolean)>("image", 1);

        goto label4;

     label3:
        $r10 = r0.<android.filterpacks.imageproc.ImageSlicer: android.filterfw.core.Frame mOriginalFrame>;

        virtualinvoke $r10.<android.filterfw.core.Frame: android.filterfw.core.Frame retain()>();

        virtualinvoke r0.<android.filterpacks.imageproc.ImageSlicer: void setWaitsOnInputPort(java.lang.String,boolean)>("image", 0);

     label4:
        virtualinvoke r0.<android.filterpacks.imageproc.ImageSlicer: void pushOutput(java.lang.String,android.filterfw.core.Frame)>("image", r18);

        virtualinvoke r18.<android.filterfw.core.Frame: android.filterfw.core.Frame release()>();

        return;
    }

    public void setupPorts()
    {
        android.filterpacks.imageproc.ImageSlicer r0;
        android.filterfw.core.MutableFrameFormat $r1;

        r0 := @this: android.filterpacks.imageproc.ImageSlicer;

        $r1 = staticinvoke <android.filterfw.format.ImageFormat: android.filterfw.core.MutableFrameFormat create(int,int)>(3, 3);

        virtualinvoke r0.<android.filterpacks.imageproc.ImageSlicer: void addMaskedInputPort(java.lang.String,android.filterfw.core.FrameFormat)>("image", $r1);

        virtualinvoke r0.<android.filterpacks.imageproc.ImageSlicer: void addOutputBasedOnInput(java.lang.String,java.lang.String)>("image", "image");

        return;
    }
}
