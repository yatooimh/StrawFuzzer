public class android.gesture.OrientedBoundingBox extends java.lang.Object
{
    public final float centerX;
    public final float centerY;
    public final float height;
    public final float orientation;
    public final float squareness;
    public final float width;

    void <init>(float, float, float, float, float)
    {
        android.gesture.OrientedBoundingBox r0;
        float f0, f1, f2, f3, $f4, f5, f6;
        byte $b0;

        r0 := @this: android.gesture.OrientedBoundingBox;

        f5 := @parameter0: float;

        f0 := @parameter1: float;

        f1 := @parameter2: float;

        f2 := @parameter3: float;

        f3 := @parameter4: float;

        specialinvoke r0.<java.lang.Object: void <init>()>();

        r0.<android.gesture.OrientedBoundingBox: float orientation> = f5;

        r0.<android.gesture.OrientedBoundingBox: float width> = f2;

        r0.<android.gesture.OrientedBoundingBox: float height> = f3;

        r0.<android.gesture.OrientedBoundingBox: float centerX> = f0;

        r0.<android.gesture.OrientedBoundingBox: float centerY> = f1;

        f6 = f2 / f3;

        $b0 = f6 cmpl 1.0F;

        if $b0 <= 0 goto label1;

        $f4 = 1.0F / f6;

        r0.<android.gesture.OrientedBoundingBox: float squareness> = $f4;

        goto label2;

     label1:
        r0.<android.gesture.OrientedBoundingBox: float squareness> = f6;

     label2:
        return;
    }

    public android.graphics.Path toPath()
    {
        android.gesture.OrientedBoundingBox r0;
        float[] r2;
        android.graphics.Path $r4;
        float $f0, $f1, $f2, $f3, $f4, $f5, $f6, $f7, $f8, $f9, $f10, $f11, $f12, $f13, $f14, $f15, $f16, $f17, $f18, $f19, $f20, $f21, $f22, $f23, $f24, $f25, $f26, $f27, $f28, $f29, $f30;
        android.graphics.Matrix $r5;

        r0 := @this: android.gesture.OrientedBoundingBox;

        $r4 = new android.graphics.Path;

        specialinvoke $r4.<android.graphics.Path: void <init>()>();

        r2 = newarray (float)[2];

        $f0 = r0.<android.gesture.OrientedBoundingBox: float width>;

        $f1 = neg $f0;

        $f2 = $f1 / 2.0F;

        r2[0] = $f2;

        $f3 = r0.<android.gesture.OrientedBoundingBox: float height>;

        $f4 = $f3 / 2.0F;

        r2[1] = $f4;

        $r5 = new android.graphics.Matrix;

        specialinvoke $r5.<android.graphics.Matrix: void <init>()>();

        $f5 = r0.<android.gesture.OrientedBoundingBox: float orientation>;

        virtualinvoke $r5.<android.graphics.Matrix: void setRotate(float)>($f5);

        $f7 = r0.<android.gesture.OrientedBoundingBox: float centerX>;

        $f6 = r0.<android.gesture.OrientedBoundingBox: float centerY>;

        virtualinvoke $r5.<android.graphics.Matrix: boolean postTranslate(float,float)>($f7, $f6);

        virtualinvoke $r5.<android.graphics.Matrix: void mapPoints(float[])>(r2);

        $f9 = r2[0];

        $f8 = r2[1];

        virtualinvoke $r4.<android.graphics.Path: void moveTo(float,float)>($f9, $f8);

        $f10 = r0.<android.gesture.OrientedBoundingBox: float width>;

        $f11 = neg $f10;

        $f12 = $f11 / 2.0F;

        r2[0] = $f12;

        $f13 = r0.<android.gesture.OrientedBoundingBox: float height>;

        $f14 = neg $f13;

        $f15 = $f14 / 2.0F;

        r2[1] = $f15;

        virtualinvoke $r5.<android.graphics.Matrix: void mapPoints(float[])>(r2);

        $f17 = r2[0];

        $f16 = r2[1];

        virtualinvoke $r4.<android.graphics.Path: void lineTo(float,float)>($f17, $f16);

        $f18 = r0.<android.gesture.OrientedBoundingBox: float width>;

        $f19 = $f18 / 2.0F;

        r2[0] = $f19;

        $f20 = r0.<android.gesture.OrientedBoundingBox: float height>;

        $f21 = neg $f20;

        $f22 = $f21 / 2.0F;

        r2[1] = $f22;

        virtualinvoke $r5.<android.graphics.Matrix: void mapPoints(float[])>(r2);

        $f24 = r2[0];

        $f23 = r2[1];

        virtualinvoke $r4.<android.graphics.Path: void lineTo(float,float)>($f24, $f23);

        $f25 = r0.<android.gesture.OrientedBoundingBox: float width>;

        $f26 = $f25 / 2.0F;

        r2[0] = $f26;

        $f27 = r0.<android.gesture.OrientedBoundingBox: float height>;

        $f28 = $f27 / 2.0F;

        r2[1] = $f28;

        virtualinvoke $r5.<android.graphics.Matrix: void mapPoints(float[])>(r2);

        $f30 = r2[0];

        $f29 = r2[1];

        virtualinvoke $r4.<android.graphics.Path: void lineTo(float,float)>($f30, $f29);

        virtualinvoke $r4.<android.graphics.Path: void close()>();

        return $r4;
    }
}
